spring:
  cloud:
    gateway:
      routes:
        - id: microservice-users
          uri: lb://microservice-users
          predicates:
            - Path=/api/users/**
          filters:
            - StripPrefix=2 #esto depende de tu ruta el mio tiene 2 niveles antes de los endpoints